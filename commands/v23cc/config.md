---
name: v23cc:config
description: Show config list
allowed-tools: Bash
model: haiku
---

**IMPORTANT: ALWAYS run the Bash command below immediately. NEVER skip it or answer from context — the script handles all edge cases itself.**

Display the full contents of `~/.v23cc/config.json`.

**Usage:** `/v23cc:config`

Run the following command via the Bash tool:

```
bash ~/.v23cc/bin/config.sh
```
